import AppShell from "@/components/layout/AppShell";
import PageHeading from "@/components/PageHeading";

export default function Auth() {
  return (
    <AppShell>
      <PageHeading
        eyebrow="Acceso"
        title="Inicia Sesión o"
        accent="Crea tu Cuenta"
        subtitle="Próximamente: formularios de acceso."
      />
    </AppShell>
  );
}
